# homebrew-tap

Homebrew formulae for [zgeoff](https://github.com/zgeoff) projects.

```sh
brew install zgeoff/tap/atc
```

The `atc` formula is written by the [atc](https://github.com/zgeoff/atc) release workflow on every release and installs the compiled binary for the current platform.
